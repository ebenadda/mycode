#!/usr/bin/env python3

import yaml

def main():
    with open("timspod.yaml", "r") as pod:
        pypod = yamml.load(pod, loader=yaml.FullLoader)
    print(pypod)

if __name__ == "__main__"
    Main()

